# Comma-separated list of origins allowed to call this API (CORS)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,https://persona-classifier.alexandre.dev

# Optional: override the SQLite file location. Defaults to backend/persona_classifier.db
# DATABASE_PATH=/data/persona_classifier.db
